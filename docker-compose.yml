services:
  falkordb:
    image: falkordb/falkordb:latest
    container_name: loom-db
    ports:
      - "6379:6379"
    volumes:
      - loom-db-data:/data
    restart: unless-stopped

volumes:
  loom-db-data:
